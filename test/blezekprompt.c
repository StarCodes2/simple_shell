#include "blezekshell.h"

/**
 * shell_prompt_display - Displays the shell prompt
 *
 * A function that displays the shell prompt, indicatong that it's ready
 * to receive user input
 *
 */
void shell_prompt_display(void) {
	blezek_print("blezek_shell$ ");
}
